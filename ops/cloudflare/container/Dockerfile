# Inert scaffold used only so Wrangler can validate the Task 7 bindings.
# Task 9 replaces this with the checksum-pinned, non-root operations image
# before any Cloudflare resource is created or deployed.
FROM scratch
LABEL org.opencontainers.image.title="OpenStays merchant operations scaffold"
