# Wavelength runtime assets

Run `npm run wavelength:runtime` to install the verified Wavelength `v0.1.0`
browser runtime here. The large generated assets are intentionally ignored by
Git; the installer pins and checks the release archive SHA-256 before extraction.
